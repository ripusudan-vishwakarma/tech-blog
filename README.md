# Ripusudan Vishwakarma — Tech Blog

Astro technical blog configured for GitHub Pages project hosting.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:4321/tech-blog/`.

The navigation and article links use the configured `/tech-blog/` base path, and article URLs use clean slugs without `.md`.
